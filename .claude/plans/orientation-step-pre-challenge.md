---
branch: orientation-step
method: challenge-loop
diff_hash: 2d1c6cf3bfd25f28a67b14003ba9962569e88de92b03495f11438ef0c6614107
converged: true
rounds: 8
date: 2026-08-15T06:45Z
---

# Challenge-loop proof: orientation-step

Eight rounds of fresh, blind reviewers (orient-review-1 through 8), each
with no access to prior findings; most ran node --test independently and
several mutation-tested the guards in scratch copies.

## Final Ledger highlights

[BLOCKER] engine/machine.js -- app-location joined the shared checks, so step 4 captioned a missing icon as a reason an agent may not run (round 1; separated at the SOURCE: appLocation rides beside the rows, both halves pinned, the counts proven not to include it)
[BLOCKER] web/index.html frForkActions -- the fork moved to step 5 but its adopt guard did not: an uncountable fleet was asserted as one that exists on the button (round 2; the same predicate as frPaintFleet, four uncountable shapes driven to the neutral pair)
[BLOCKER] server.test.js -- the token test could not fail on the token half of its name (round 5; the guard's one reachable job is the tested scenario, both guard copies red on deletion)
[BLOCKER] web/index.html -- the generation guard had no production trigger (round 6; frGo and frClose really bump it, and round 7 pinned the TRIGGERS after proving their deletion stayed green)
[WARNING] the step-5 pre-paint was byte-identical to the engine's unknown row, so no picture could tell the placeholder from the answer (round 1; three distinguishable wordings, asserted everywhere)
[WARNING] the live region was created by the write that filled it (round 3; static markup, pinned; the dock joined it in round 8 because its instruction changes with the answer)
[WARNING] answers with nothing to SAY rendered a confident blank tick (round 5; title and detail required, tested)
[WARNING] shots depended on the machine that rendered them (rounds 6-7; every shot-bearing payload pinned AND engine-generated with a control, including the checking state that nothing else renders)
[WARNING] first-folder-wins provenance (round 8; RECORDED as the designer's spec-level follow-up with the evidence that "found one, but not ours" is now a distinct honest answer -- a pinned-spec change, not tonight's edit)
[CONVENTION] the Dock instruction tracks whether a folder was FOUND on every path, and the closing sentence stopped claiming a screen that deliberately never returns
[NIT] every real-machine test call site sandboxed; counts and comments told the truth by the end
[STRENGTH] -- could-not-look is the honest LAST answer (an unreadable first folder does not eat a find in the second, proven with a real mode-000 folder, root-skipped loudly)
[STRENGTH] -- the drag-not-Keep-in-Dock guard asserted on all committed shots in both schemes and in the live click walk

## Verification at close

node --test: 740 pass. render-first-run: no rendering problems, 28
committed shots all pinned and machine-independent, the contrast
instrument proving it can fail via its planted control. click-first-run:
all clear against the live sandboxed route, including the proof that the
live answer (not the fallback) painted. Spec fidelity held: the fifth
step's copy verbatim where the spec spoke, deviations recorded as
amendments for the designer (the folderless drag variant, the
brings-Kosmos-back sentence).
