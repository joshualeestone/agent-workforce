# v014 -- version bump to 0.1.4

## Why

The first-run rebuild (#56) is merged: the whole setup flow now matches
the pack screen for screen, with Josh's ruled wording, the Success
screen's "Show me where it is" button, Escape as the one exit, and the
count-only fleet screen. Josh checks the flow off by running a real
uninstall-and-reinstall on the new mini, which needs a released
version, so 0.1.4 carries #56 (and #55's icon-refresh fix, merged
after 0.1.3 was cut).

## What

One line: package.json version 0.1.3 -> 0.1.4. The release itself
(bundle, latest.json, /versions entry, /setup republish) happens on
the site side after this merges, same shape as release-013.

## Verification

Suite green on the bumped tree; the bundle build and live sweep happen
in the site release plan.
