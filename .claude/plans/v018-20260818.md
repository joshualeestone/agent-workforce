# v018: 0.1.8 reaches users

The release that carries today's work to `kosmos update`:
- dark mode reaches the cards (#71, the pack's dark k-token set), one
  dark attention red (#72)
- the project room end to end: the a2a engine hygiene (#73), room posts
  with marked background delivery and the arrivals valve (#74), the
  pjone room screen with operator posting and per-recipient receipts
  (#75)
- plus last night's post-0.1.7 merges already on main: the PM briefing
  and message screens (#68, #69), the pack card grid (#70)

One line: package.json 0.1.7 -> 0.1.8. The bundle, latest.json, the
site versions entry, deploy, and the proven live-sweep follow on the
site side per the release runbook.
