# limit-words: Mona Lisa's pass on the three attributed drafts

Her rulings (2026-08-18 ~1:42 PM), applied verbatim:
1. The card's category label: "Conversations" -> "Agent conversations"
   (whose conversations? one word removes the ambiguity).
2. The high-tier cost line ships as written (the hedge is honest; a
   number we invented would be worse).
3. The told-only sentences end "Kosmos did not step in, because you
   have the limit turned off." (the honest-states shape: what happened
   and why in one line; "instead" assumed the reader holds a setting
   they changed an hour ago). The pair record's agent-facing variant
   says "because the limit is turned off". All four sites (two engine
   becauses, two composed renderings) and their test pins move
   together.

Pass note: the review confirmed the sweep complete (zero hits on all
four old phrases; each string traced to its actual reader with the
correct grammar per audience). One stale comment above the pins fixed.
Recorded follow-up, pre-existing and out of this ruled scope: the
On-state room refusal returns the person-addressed everyone-sentence
verbatim to the refused AGENT; same audience-split class, for the
next messaging copy pass.
