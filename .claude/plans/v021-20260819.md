# v021: version bump for the 0.2.1 release

Date: 2026-08-19 ~9:26 PM. Branch `v021`. Josh's standing ruling
in-channel the same evening: "we are in the middle of building a beta
app with no users, if we have a PR ready, we merge it we never ever
wait on me to look at it or review." Merged #89 under it, and this
bump is what carries #89 to installed boards.

- package.json 0.2.0 -> 0.2.1 (single-sourced; nothing else in the
  repo hardcodes the version, verified by grep before the edit).

## Release content (already merged)

- #89 screen-pass: the build's half of Josh's 2026-08-19 screen review.
  Model known-at-create is read back instead of "Unknown Model"; six
  install-screen provider discs that were styled at 0x0 render; a
  top-level tab click lands at the top of its section; agent detail
  boxes in the asked-for order; unknown memory says so visibly on card,
  row and detail, where a blank cell had read as "0% used"; dark-mode
  cards no longer render darker than the page. Its own review loop did
  NOT converge (eleven rounds, six nits deferred with measurements) and
  says so in the PR body.

## Pipeline after merge

tools/build-kosmos-bundle.sh -> chaoskosmos-site dist/ (tarball +
.sha256 + latest.json at 0.2.1) -> versions.html entry -> vercel deploy
--prod (the site is CLI-deployed, pushing the branch publishes nothing)
-> tools/live-sweep.sh -> fetch installkosmos.com/dist/latest.json and
confirm it serves 0.2.1 -> tell Josh in-channel.

## Deliberately not in this slice

The six deferred nits from #89, which are recorded there with
measurements. The `answer-panel` branch (14 commits, last touched
2026-08-19 14:47 at challenge-loop iteration 10) is NOT in this
release and has no PR; it is named here so it is not mistaken for
shipped.
