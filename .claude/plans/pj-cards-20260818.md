# pj-cards — All Projects to the pack (view C)

Built against the committed reference packshots/project-family/
pack-project.png (freeze 17c rendered fresh, Mona Lisa 4678724), the
first chunk of Josh's page-by-page walkthrough.

- The panel header becomes the pack's statsrow: New-project dashed tile
  (id pj-new keeps the click contract), Projects + Needs-you counts
  derived from the same rows painted (the alert tile hides at zero and
  under a blind roster, when the count is a claim we cannot make), the
  view toggle. Grid is the pack's default; the stored choice still wins.
  The lede sentence left with the header (pack draws none; its
  definition survives in the empty state and the add flow).
- .pj-row keeps its name (click contract, drives) and takes the pcard
  look: name + state pill, slug chip (the FOLDER's real basename, what
  the person can find in Finder, not the engine's collapsed id), member
  faces (avatar when the TIED agent has one -- hasAvatar is gated on
  tied in the engine, a borrowed pane must not lend the card somebody
  else's photograph -- initials otherwise), agent count, description.
- The pill claims only what the counts support (pinned at its honesty
  boundaries): Needs you > Working > No agents yet > Can't tell (blind
  roster or unseen members) > Nothing running (only when every member
  was seen). The cannot-see and folder warnings stay in words on the
  card.
- The white panel shell retired (cards on the paper ground, the
  settings precedent); 23rem grid min for the pack's three-across.

## Recorded

- The description pin moved with its arm (pj-desc -> pjdesc).
- View D (One project) is NOT this chunk and is bigger than a restyle:
  Mona Lisa ruled the build's switchboard vs the pack's project
  conversation two different interaction models. Splinter's standing
  constraint for it: project-thread writes are OPERATOR-only; agent
  output reaches the thread as an attributed reply to a person's
  request, never an unprompted post (Josh can override).
- The Enter-to-confirm copy fix and raw-pane-behind-Engineering-mode
  ride the pjone/clean-chat chunks.

## Passes 2 and 3 (each found what the previous fix introduced)

- Pass 2's blocker: the ring tint never rendered -- an SVG presentation
  attribute sits below every author CSS rule and .avatar-bg declares
  fill; moved to inline style. The disc became the pack's own seven
  tint-and-ink pairs (their contrast verified 5.6-6.1:1 at chip size by
  pass 3) under the pack's FNV hash: the disc section's own polynomial
  collides four of six anagram pairs at mod 7 (31 and 997 interact badly
  with mod 7) -- a genuine pack defect, recorded there (e0112fd), the
  arithmetic in the entry so nobody reverts it as a preference. The
  anagram TEST is statistical (forty corpus-derived pairs, bounded above
  chance and below the 40/40 an order-independent hash produces) because
  a fixed-pair assertion punishes a correct hash for ordinary luck.
- Pass 3's blocker: my own a11y wrapper blockified inside the flex row
  and stacked multi-member faces vertically -- geometry no text check
  sees; the wrapper is now its own flex row, proven by a bounding-rect
  check (one row, overlapping) rather than a screenshot glance. The
  detail header joined the shared disc for real (the comment had claimed
  four surfaces while three shared it); the stale pj-desc comment
  claiming a nonexistent harness assertion was rewritten to the truth;
  the disc test derives the palette from the page (a copied bucket count
  keeps certifying mod 7 after the palette moves) and pins
  TINTS.length === INKS.length.
- Splinter's tile-draw boundary: the tiles are driven at the WRITE, so a
  tile that computes and never draws cannot pass; blind-roster
  needs-you added to the pill boundaries.

## Passes 4-5 (2026-08-18 morning)

- Pass 4's blocker was the canonical drive itself: render-projects.js
  still described the pre-pack page. Rewritten to prove this one, and
  RUN to green (all states, AA both schemes, 18 shots, no console
  errors). What running it surfaced, each fixed in the app or the
  drive as the finding demanded:
  - the pack draws the two descriptions at different sizes on purpose
    (.pc-t .875rem on the card, .pjdesc .9375rem on the detail), so the
    same-token equality retired for two exact pack pins; the card class
    renamed pjdesc -> pc-t because the pack spends .pjdesc on the
    detail and the collision would mis-style the pjone rebuild.
  - the detail description had been orphaned at the retired 12px
    callout token, and the pack's fixed ink on it measured 1.69:1 in
    dark mode: it takes the pack's sizes with the ADAPTIVE token (the
    fixed inks belong to the fixed-light cards).
  - an unbroken 200-char description slid under the card edge:
    overflow-wrap on both descriptions.
  - remove/archive/folder facts are proven where the pack moved them
    (behind Project settings); the always-hidden raw-path element and
    its dead CSS retired.
  - a poll landing while settings is open repaints the state-derived
    half only (paintSettingsFacts): repainting the whole view would eat
    a rename mid-type (the round-33 draft-eater class), repainting
    nothing left "Restore failed. Try again" standing after recovery.
  - the three-card grid never scrolled, so stickiness was exercised on
    fillers made through the real create route; scope independence
    restated as before == after now that grid is the projects default.
  - the faces-geometry guard was proven to FAIL on the exact pass-3
    stacking defect before its green was trusted.
- Pass 5 (blind): zero blockers. Its two warnings both landed:
  - the AA sweep now measures every card token (name, pill incl. a
    toggled-on needs-you variant, slug, count, disc initials). The
    extension immediately false-failed on the slug chip -- the sweep's
    background walk read a 5% translucent wash as opaque near-black --
    so bgOf now COMPOSITES layers, proven both directions (the false
    red gone; a genuinely failing slug ink reds at 1.49).
  - a read failure is reported on the settings view (#pjs-read-msg),
    the symmetry the poll-repaint addition owed.
  - its "adjacent pre-existing" nit was a live bug taken here: pjs-save
    read loadProjects' truthy recorded-failure answer as a good read
    (and its || retry could never fire), so stale PROJECTS repainted
    under "Saved."; now the sibling's retry shape + PJ_READ_FAILED.
- Process note, cost paid twice now: a perturbation restore via git
  checkout ate five uncommitted fixes (memory: commit-before-perturb).
  Reapplied and committed; suite 846/846 and the drive green on the
  committed bytes.
