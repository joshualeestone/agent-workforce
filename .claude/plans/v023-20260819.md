# v023: version bump for the 0.2.3 release

Date: 2026-08-19 ~11:07 PM. Branch `v023`. Third release of the night under
Josh's 21:40 ruling to ship chunks as they finish.

- package.json 0.2.2 -> 0.2.3 (single-sourced).

## Release content (already merged)

- #93 project-page, second slice: an agent can be renamed (the route allowed it
  since round 32 and no screen ever sent one); Project documents, list and
  open, with an engine gate that refuses a symlink escape; the folder name in
  Project settings, which had no CSS rule at all; two backwards create-flow
  comments; and the contrast sweep's second `.flabel` dependency.

## Pipeline after merge

build-kosmos-bundle.sh -> chaoskosmos-site dist/ -> versions.html entry ->
vercel deploy --prod -> read latest.json AND the version inside the SERVED
tarball back, with the previous tarball as a positive control -> tell Josh.

## Deliberately not in this slice

The "Talk to one of them" delete, still held behind the room carrying an
agent's blocking question. Mona Lisa confirmed tonight that the whole chunk is
already designed across two files, and that the port takes the PACK's layout
with the ENGINE's words, because chat.js `waitingNote` has five delivery states
to the pack's two. Task status. The documents view-all as a real screen.
