# three-tabs — the pack's three tabs, ruled

Josh, 2026-08-17 ~11:09 PM in-channel: "Drop your the packs 3 tabs."
Mona Lisa's coupling rule (settled earlier): the tab and the name flip
TOGETHER — "Projects" only means anything in a three-tab world.

- The Joint Projects tab and its panel go. The panel was a coming-later
  placeholder (no functionality, no storage); its copy, including the
  part-we-would-charge-for honesty note, lives in git history for the
  day sharing is built.
- "My Projects" becomes "Projects" (tab and panel h2; data-tab and every
  id unchanged, so the JS and drives keep addressing them).
- THE REMOVAL'S INVISIBLE HALF, checked positively before the tab came
  off (Mona Lisa's rule: a removal is two changes): a project with TWO
  agents on it lands in the ONE Projects list with both agents readable
  (engine-level positive check, sandboxed store: created leo+mara on one
  project, listed, found, both present). Joint was never a second list;
  multi-agent projects always lived in Projects.
