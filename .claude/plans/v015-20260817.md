# v015 -- version bump to 0.1.5

## Why

The ruled-four wave (#59) is merged: the setup dot field, the tab
icons, the permissions acceptance at install, the inline update
notice, and the MIT license. 0.1.5 is also the FIRST production test
of the update path itself: Josh's mini runs 0.1.4 untouched, and this
release must appear as the notice on his open board (four legs: the
running board notices, the toast shows without restart, one click
updates with agents still running, and an agent verifiably survives
the update). The permissions fix gets its clean pass/fail on the same
machine: after updating, messaging an agent must never show the wall.

## What

One line: package.json 0.1.4 -> 0.1.5. Release mechanics on the site
side, same shape as release-014.
