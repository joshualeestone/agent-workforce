# v020: version bump for the 0.2.0 release

Date: 2026-08-18 ~9:03 PM. Branch `v020`. Josh's ship word in-channel
(message 1539454426710941707, "Let's ship it!").

- package.json 0.1.9 -> 0.2.0 (single-sourced).

## Release content (already merged)

- #82 group-reasons: plural siblings for the folder verdicts; Remove
  from project.
- #83 reach-the-command: the taught command runs on this machine; the
  installer wires PATH; stale blocks heal; agents say failed sends.
- #84 update-control: Check now, 15-minute check, honest reachability
  states, the finish line naming what landed.

## Pipeline after merge

tools/build-kosmos-bundle.sh -> site dist/* + latest.json + versions
entry (drafted mine, flagged for Mona Lisa) -> vercel deploy --prod ->
tools/live-sweep.sh -> served-sha check -> channel confirmation, with
the correction that 0.1.9 installs still poll six-hourly (the fifteen-
minute check ARRIVES with this build; my ship note overclaimed).
