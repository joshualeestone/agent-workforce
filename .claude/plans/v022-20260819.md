# v022: version bump for the 0.2.2 release

Date: 2026-08-19 ~10:13 PM. Branch `v022`. Under Josh's 21:40 ruling to ship
chunks as they finish rather than batching them for morning.

- package.json 0.2.1 -> 0.2.2 (single-sourced; grep confirms no other file
  hardcodes the version).

## Release content (already merged)

- #91 project-page: the restore-to-pack half of Josh's 2026-08-19 markup.
  Title size, members hint removed, the pack's 20/1fr/20 column split, the
  three column headers as h3.dlab, the room thread's box and background at the
  pack's radius, the pack's composer with the controls inside the box and a
  textarea that grows, a thread that fills the page, and three pack values that
  had an exact token and used another.

## Pipeline after merge

tools/build-kosmos-bundle.sh -> chaoskosmos-site dist/ (tarball + .sha256 +
latest.json at 0.2.2) -> versions.html entry -> vercel deploy --prod (CLI
only; pushing publishes nothing) -> fetch installkosmos.com/dist/latest.json
and read 0.2.2 back, plus the version INSIDE the served tarball with the
previous tarball as a positive control -> tell Josh in-channel.

## Deliberately not in this slice

Project Documents and task status (net new, no pack to match). The delete of
"Talk to one of them", held behind the room learning to carry an agent's
blocking question. The `answer-panel` branch. The known red in
render-projects.js, which is `.pj-told` and needs a failed-tell fixture.
