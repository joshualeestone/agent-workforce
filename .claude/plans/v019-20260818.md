# v019: version bump for the 0.1.9 release

Date: 2026-08-18 ~4:41 PM. Branch `v019`. Josh's ship word in-channel
(message 1539388419719831605, "Sorry, yes 0.1.9").

## What

- package.json version 0.1.8 -> 0.1.9 (single-sourced; the app reads its
  version from here).

## Release content (already merged, not in this diff)

- #77 talk-limit: the conversation limit as the person's control.
- #78 limit-words: Mona Lisa's copy pass on the limit card.
- #79 eng-mode: Engineering mode real (Off hides the raw window, On
  reveals both surfaces; safety never gated).
- #80 room-polish: receipt pill dark borders, roster verdict collapse,
  valve band timestamp gap.

## Pipeline after merge

tools/build-kosmos-bundle.sh -> site repo dist/* + latest.json +
versions.html entry (merge minute from THIS repo's log; entry drafted
mine, flagged for Mona Lisa's bless-or-replace) -> vercel deploy --prod
-> tools/live-sweep.sh -> served-sha == built-sha check -> channel
confirmation.
