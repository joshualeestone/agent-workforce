# v026: version bump for the 0.2.6 release

Date: 2026-08-20 ~12:12 AM. Branch `v026`. Sixth release.

- package.json 0.2.5 -> 0.2.6 (single-sourced).

## Release content (already merged)

- #99 project-page, fifth slice: the file paths agents cite in a project room
  become the pack's `.ref` chip with a "Show me" button, for the paths that
  name a file the project's folder actually contains.

## ⚠️ The two traps this release walked into, both caught

1. The version regression, FOURTH occurrence: branch 0.2.4, main 0.2.5.
2. A commit that shipped CONFLICT MARKERS, because a resolve script died and
   the `git add`/`git commit` on the following lines ran anyway. Caught by a
   CSS-structure test, not by a merge check. The resolve is now gated on a
   proven zero-marker count.

## Pipeline after merge

build -> chaoskosmos-site dist/ -> versions.html -> vercel deploy --prod ->
read latest.json AND the version inside the SERVED tarball, with the previous
tarball as a positive control, and confirm the new markup is in the bundle.
