# v028: version bump for the 0.2.8 release

Date: 2026-08-20 ~12:44 AM. Branch `v028`. Eighth release.

- package.json 0.2.7 -> 0.2.8 (single-sourced).

## Release content (already merged)

- #103 project-page, seventh slice: `.codeb`. A fenced block in a project-room
  message is drawn as a monospaced, whitespace-preserving box. Nothing inside
  a fence is linked, an unclosed fence stays prose, and NO caption is emitted
  because nothing in the product can name a block's source.

## ⚠️ The version trap, sixth occurrence

Branch 0.2.6, main 0.2.7. Six for six caught by comparing both sides before
merging. Nothing pins the version to a number, so no test will ever fail on it.

## Pipeline after merge

build -> chaoskosmos-site dist/ -> versions.html -> vercel deploy --prod ->
read latest.json AND the version inside the SERVED tarball, with the previous
tarball as a positive control, and confirm the new markup is in the bundle.
