# v027: version bump for the 0.2.7 release

Date: 2026-08-20 ~12:31 AM. Branch `v027`. Seventh release.

- package.json 0.2.6 -> 0.2.7 (single-sourced).

## Release content (already merged)

- #101 project-page, sixth slice: `.xlink`. A URL in a project-room message
  becomes a real link, with `rel="noreferrer noopener"` on the one click in
  this product that leaves the machine. Dangerous schemes are never matched,
  because the test is a literal `http(s)://` prefix rather than a URL parse.

## ⚠️ The version trap, fifth occurrence

Branch 0.2.5, main 0.2.6. Same silent failure every time: a branch cut one
version behind reverts package.json when merged, and every board on the newer
version reads that as "no update available". Nothing pins the version to a
number, so no test fails. Five for five caught by comparing both sides first.

## Pipeline after merge

build -> chaoskosmos-site dist/ -> versions.html -> vercel deploy --prod ->
read latest.json AND the version inside the SERVED tarball, with the previous
tarball as a positive control, and confirm the new markup is in the bundle.
