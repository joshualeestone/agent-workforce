# v017 — the 0.1.7 bump

One line: package.json 0.1.6 -> 0.1.7, so the release a fresh install
receives carries three-tabs (#63) and agent-to-agent messaging (#64).
Both merged tonight under their own plans and proofs; this branch is the
version stamp only, per the release convention (the bundle and the
site's latest.json follow from merged main).
