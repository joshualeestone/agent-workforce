# v024: version bump for the 0.2.4 release

Date: 2026-08-19 ~11:40 PM. Branch `v024`. Fourth release of the night.

- package.json 0.2.3 -> 0.2.4 (single-sourced).

## Release content (already merged)

- #95 project-page, third slice: member faces on the project column, task
  status in all four of its states (it existed and rendered in one), and a
  role hint that names the outcome in four words.

## Pipeline after merge

build -> chaoskosmos-site dist/ -> versions.html -> vercel deploy --prod ->
read latest.json AND the version inside the SERVED tarball, with the previous
tarball as a positive control, and confirm the new markup is in the bundle.

## Deliberately not in this slice

The "Talk to one of them" delete (held on a safety constraint; its prerequisite
branch `answer-panel` is current with main and green at 975 tests but its own
challenge loop never converged). The documents source question, which needs
Josh because two of his own sentences disagree. The composer's `+`.
