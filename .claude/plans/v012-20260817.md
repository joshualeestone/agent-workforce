# v012: the version bump for tonight's release

Josh's directive (2026-08-16 ~11:10 PM): get to a new install fast;
Splinter's reorder: ship 0.1.2 FIRST so the morning install carries what
is already merged (sleep-settings button #48, tasks column #49, the
update pipeline #47 that will announce it to 0.1.1 boards). One line:
package.json 0.1.1 -> 0.1.2. The bundle builds from this commit and the
site's dist + latest.json + /versions entry (Mona Lisa's pen) publish
separately.
