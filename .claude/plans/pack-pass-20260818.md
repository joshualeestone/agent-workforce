# pack-pass — the scrappy pack pass across the remaining app screens

Date: 2026-08-18 (overnight). Goal set by Josh in-channel ~9:58 PM CDT:
"a scrappy version of the full design across all the pages in a new
installation to test in the morning."

## Source of truth

Build from CURRENT-FREEZE in ~/work/Josh-Brain/Projects/kosmos-design/ —
never a remembered filename. At build time that is
kosmos-app-style.FROZEN-2026-08-17c.html, sha256
44935a5fa0b6c3bf2c21a5ba83f800dcafdfe675248848812b21950fd07bdfaf,
verified against the CURRENT-FREEZE record before the first edit.

## Rulings in force (all from the 2026-08-17 channel, recorded verbatim
## in the handoff and thread)

1. SCRAPPY MANDATE (Josh): app design lands fast so he can see and test
   it; take the 80 percent and FLAG gaps instead of polishing. A flagged
   gap is a task; a polished guess is a decision nobody made (Mona Lisa).
2. FIRST RUN, SETUP, INSTALLER, LOGIN UNTOUCHED (Mona Lisa, agreed):
   first run is already AT pack quality (0.1.4/0.1.5, ruled screen by
   screen); a scrappy pass over the one finished thing can only lose
   ground. The batch starts where the board appears.
3. JOINT PROJECTS DEFAULT (pending Josh's keep-or-drop): four tabs,
   "My Projects" NAME UNCHANGED, both restyled to the pack. The tab and
   the name flip TOGETHER on Josh's word ("Projects" bare only works in a
   three-tab world).
4. Review depth: ONE blind pass at the end (per Josh's rigor split:
   site light / app fast / first-run deep), blockers fixed, then ship.
   Not the rounds-to-convergence harness the board got.

## Scope: app surfaces mapped to pack views

- Agent detail  <- pack view B "One agent" (the board branch already
  moved the machine-name disclosure + because sentence here; this pass
  restyles the shell: header, meta, fields, instruction editor).
- Create flow   <- pack view I "Create an agent" (+ its "Making leo…"
  progress state).
- My Projects   <- pack views C "All projects" / D "One project" /
  L "No projects yet" (+ F "New project" where the app has the flow).
- Joint Projects <- same grammar as C/D per ruling 3 (no pack view
  exists for it; FLAG: pack draws three tabs).
- Settings      <- pack view K "Settings" (freshest freeze content:
  Engineering mode card captured 17b, full-width cards 17c).
- Remove & restore <- pack view J (the board branch restyled the
  removed list already; verify parity, take deltas only).

## Explicitly out (flagged as gaps -> tasks, not built tonight)

- Task views (pack G "One task" / H "All tasks"): no app surface exists.
- Attach flow (pack M "Adding a file") beyond what project chat has.
- Theme switch: stays deferred to #40 (board-branch ruling carries).
- Engineering mode SETTING (card #60 owns the behavior; the settings
  restyle draws the pack's card only if it can ride without new
  behavior, else flagged).

## Deliverable tail

Suite green throughout (the 821 tests are the behavior guard; this pass
is styling and markup, not logic). One blind review round, fix
blockers, proof file, PR (reviewer joshualeestone), merge on standing
authorization, cut the release bundle, post the fresh-install command
in-channel for the morning test.
