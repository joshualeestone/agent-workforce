Throttle-settings chunk spec (Josh in-channel 2026-08-18 1:03 PM CDT):
1. Switch: "Throttle agent conversations to save on LLM charges" On/Off.
   Off legitimate (open-source models). Off state copy per Mona Lisa's
   honest-states rule.
2. When On: dropdown, N per hour, low -> generous tiers. One dial; the
   engine converts to per-pair and per-room-arrival internals.
3. Cost warning beside Off / high tiers.
Floor question resolved: Off exists, no hidden floor.
Mona Lisa does the wording pass before ship.
4. Ships ON with a default value set (Josh, 1:03 PM follow-up). The
   current engine numbers become the default tier.

Mona Lisa's copy (1:03 PM, VERBATIM for the build):
  Heading: "Limit how much your agents talk to each other"
  Hint: "Every message between your agents uses your AI plan. If you
    run a model that does not charge you, turn this off."
  [ On / Off ]
  when On: "Stop them after" [ 10 / 20 / 40 / 100 ] "exchanges an hour"
  when Off (warning): "Your agents will keep talking as long as they
    have something to say. If your provider charges per use, that can
    add up while you are away."
Rules: no "LLM"/"throttle" in copy; Off IS unlimited (no unlimited tier
in the dropdown); the warning renders only in the Off state and at the
high end, never permanently. Ships On, default tier set (Josh). Unit
shown: exchanges/hour; engine converts to pair cap and room arrivals.

Splinter (1:04 PM): the valve carries TWO jobs, cost AND convergence
("without landing" is its own sentence). Off must not silently lose the
only signal that two agents are circling. Options he named: keep a
convergence surface that fires regardless of the cost setting (one
interruption, costs nothing), or the Off state says plainly that Kosmos
will stop telling them when a conversation is going nowhere. Angel +
Mona Lisa to decide in the build.
Mona Lisa (1:04 PM): label is HER copy (no "throttle"/"LLM"); Josh's
ship-on-default folds in; UNIT WARNING: dropdown is per hour, engine
windows are per half hour -- the number on screen must be the number
they get; conversion placement is Angel's call.
Mona Lisa FINAL (1:04 PM, supersedes her Off copy above): the two jobs
split. The counter ALWAYS tells the person (not configurable, same rule
as "we could not look"); the switch decides only whether Kosmos also
STOPS them. On = tells you and stops them (default). Off = tells you
and lets them continue. Off copy verbatim: "Your agents will keep
talking as long as they have something to say. Kosmos will still tell
you when a conversation stops going anywhere, but it will not step in."
Engine consequence: the valve refusal path splits into notify (always)
and stop (setting-gated).
