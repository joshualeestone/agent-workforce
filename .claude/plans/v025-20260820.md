# v025: version bump for the 0.2.5 release

Date: 2026-08-19 ~11:55 PM. Branch `v025`. Fifth release of the night.

- package.json 0.2.4 -> 0.2.5 (single-sourced).

## Release content (already merged)

- #97 project-page, fourth slice: the "+ Add member" button takes the pack's
  quiet, full-width treatment, and the two controls Josh circled that the pack
  never drew are recorded as unanswerable rather than invented.

## Pipeline after merge

build -> chaoskosmos-site dist/ -> versions.html -> vercel deploy --prod ->
read latest.json AND the version inside the SERVED tarball, with the previous
tarball as a positive control, and confirm the new markup is in the bundle.

## ⚠️ The trap this bump exists to avoid, third occurrence tonight

A branch cut one version behind main silently REVERTS package.json when merged,
and every board on the newer version reads that as "no update available".
Nothing in 938 tests pins the version to a number. Caught on #93, #95 and #97
by comparing both sides before merging. It is a habit, not a check, and it is
the only thing standing between an evening's work and an invisible release.
