{
  "name": "agent-workforce",
  "version": "0.1.0",
  "private": true,
  "description": "A small team of assistants that runs on your own computer.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test engine/*.test.js *.test.js",
    "type-check": "echo 'plain JavaScript, nothing to type-check'",
    "lint-fix": "echo 'no linter configured'",
    "build": "echo 'no build step: the app runs from source'"
  }
}
